export interface Category {
  _id: string;
  title: string;
  description?: string;
}

// Simplified version for display purposes
export interface CategoryDisplay {
  _id: string;
  title: string;
  description?: string;
} 